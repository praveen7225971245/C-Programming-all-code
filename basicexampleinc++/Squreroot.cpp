#include <iostream>
#include <cmath>
using namespace std;

int main(){
    double n ,result;
    cout<<"Enter the number to find the square root: ";
    cin>>n;
    
    result = pow(n,0.5);
    cout<<"the squre root of no. "<<result;
    return 0;
    
}